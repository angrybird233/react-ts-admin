
import {{ properCase ename }} from './{{ ename }}.tsx';

export default {
  path: '/{{ pname }}', // （*必填）
  meta: {
    type: {{ type }}, // 1: 菜单  2: 视图  3: 菜单+视图
    title: '{{ cname }}', // 菜单以及权限验证所显示的中文名 （*必填）
    key: '{{ key }}',
    filePath: '{{ pname }}', // 模块路径
    menuIcon: '', // 左侧菜单的icon
    description: '{{ cname }}', // 模块描述
    keepAlive: false, // 是否缓存
    requiresAuth: true, // 是否需要权限验证
    isShowMenu: true // 是否在菜单中显示
  },
  children: [],
  element: <{{ properCase ename }} />
}
